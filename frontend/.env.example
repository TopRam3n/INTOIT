# Spring Boot backend URL (optional - falls back to demo mode if unreachable)
VITE_API_URL=http://localhost:8080/api

# Snowflake ingestion URL (optional)
VITE_SNOWFLAKE_URL=https://your-account.snowflakecomputing.com
